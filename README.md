## How to run

1. Clone the repository.
2. Run `npm install`.
3. Run `npm run dev` for development.

## Enter to DB shell

1. Run `npm run db`

### Usefull commands

- `\dt`: list tables.
- `\d table_name`: show table with details.